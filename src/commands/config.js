/**
 * 설정 관련 명령어 로직을 담당합니다.
 * Phase C에서는 호출 여부 확인을 위한 로그만 출력합니다.
 */

export const setMode = (mode) => {
  console.log(`[DEBUG] setMode 호출됨: ${mode}`);
};

export const setLanguage = (lang) => {
  console.log(`[DEBUG] setLanguage 호출됨: ${lang}`);
};
