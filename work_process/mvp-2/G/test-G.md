# Test G: localLLM 모델 목록 조회 테스트

## 테스트 항목
1. **모델 리스트 반환 확인**:
   - `listModels(config)` 호출 시 서버에 로드된 모델명(예: `llama3`, `mistral`)들이 배열로 반환되는지 확인.
2. **응답 파싱 검증**:
   - 서버의 JSON 응답 형식이 복잡하더라도 필요한 `id` 필드만 정확히 추출하는지 확인.
3. **빈 목록 처리**:
   - 서버에 로드된 모델이 없을 때 적절한 빈 배열 또는 안내 메시지를 반환하는지 확인.
